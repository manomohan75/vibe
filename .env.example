# API server
# Adjust the port if you change the Express server port.
PORT=4000

# Postgres connection (Neon URL works both locally and on Vercel)
# Include sslmode=require in the string.
DATABASE_URL=postgresql://USER:PASSWORD@YOUR-NEON-HOST/DBNAME?sslmode=require
# Alternatively, set NEON_DATABASE_URL instead of DATABASE_URL.
# NEON_DATABASE_URL=

# Frontend overrides (optional in local dev)
# VITE_API_URL=http://localhost:4000
# CLIENT_ORIGIN=http://localhost:5173
